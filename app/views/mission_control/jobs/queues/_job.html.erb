<tr class="job">
  <td>
    <%= link_to application_job_path(@application, job.job_id, filter: { queue_name: job.queue }), class: "mc-job-link" do %>
      <%= job_title(job) %>
    <% end %>
    <div class="mc-meta has-text-grey">Enqueued <%= time_distance_in_words_with_title(job.enqueued_at.to_datetime) %> ago</div>
  </td>
  <td>
    <% if job.serialized_arguments.present? %>
      <span class="mc-code-inline is-family-monospace">
        <%= job_arguments(job) %>
      </span>
    <% end %>
  </td>
</tr>
