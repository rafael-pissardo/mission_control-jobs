<tr class="worker">
  <td>
    <%= link_to "worker #{worker.id}", application_worker_path(@application, worker.id), class: "mc-job-link" %>
    <br />
    <%= worker.name %>
  </td>
  <td><%= worker.hostname %></td>
  <td>
    <% worker.jobs.each do |job| %>
      <div>
        <%= link_to job_title(job), application_job_path(@application, job.job_id), class: "mc-job-link" %>

        <% if job.serialized_arguments.present? %>
          <div class="mc-code-inline is-family-monospace is-size-7"><%= job_arguments(job) %></div>
        <% end %>
      </div>
    <% end %>
  </td>

  <td><div class="mc-meta has-text-grey"><%= time_distance_in_words_with_title(worker.last_heartbeat_at) %> ago</div></td>
</tr>
